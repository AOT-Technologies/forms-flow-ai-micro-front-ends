/* istanbul ignore file */
import { RequestService } from "@formsflow/service";

// import API from "../../endpoints/index";

export const fetchdashboards = (callback, errorHandler) => {
     console.log("start fetch dashboard") // template code
};
