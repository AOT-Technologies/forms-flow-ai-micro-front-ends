# Exclude auto-generated lockfiles from analysis — their highly repetitive,
# machine-written structure trips SonarCloud's duplication check on any PR
# that bumps dependency versions (e.g. FWF-6447 dependabot security fixes),
# even though there is no duplicated logic to fix.
sonar.exclusions=**/package-lock.json
